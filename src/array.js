"use strict";

/**
 * Generate a range of numbers
 *
 * @param {number} lowerbound Beginning of the range
 * @param {number} upperbound End of the range
 * @return {number[]} Range between the lower and upper boundaries
 * @example
 * range(1, 5) // [1, 2, 3, 4, 5]
 * range(5, 1) // [5, 4, 3, 2, 1]
 * range(-1, -5) // [-1, -2, -3, -4, -5]
 * range(-5, -1) // [-5, -4, -3, -2, -1]
 */
function range(lowerbound, upperbound) {
    // ...
}

module.exports = { range };
